<%= form_with(model: product, local: true) do |form| %>
<% if product.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>
	
	<ul>
		<% product.errors.full_messages.each do |message| %>
		<li><%= message %></li>
		<% end %>
	</ul>
</div>
<% end %>

<div class="field green-text bold">
	<%= form.label 'Nombre:' %>
	<%= form.text_field :name, id: :product_name %>
</div>

<div class="field green-text bold">
	<%= form.label 'Descripción:' %>
	<%= form.text_area :description, id: :product_description %>
</div>

<div class="field green-text bold">
	<%= form.label 'Precio:' %>
	<%= form.text_field :price, id: :product_price %>
</div>

<div class="field green-text bold">
	<%= form.label 'Stock:' %>
	<%= form.number_field :stock, id: :product_stock %>
</div>

<div class="field green-text bold">
	<%= form.label 'Stock Minimo:'%>
	<%= form.text_field :stock_minimum, id: :product_stock_minimum %>
</div>

<div class="field green-text bold">
	<%= form.label 'Impuesto:' %>
	<%= form.number_field :tax, id: :product_tax %>
</div>

<div class="field green-text bold">
	<%= form.label 'Imagen:' %>
	<%= form.file_field :image, id: :product_image %> <br>
	<%= form.label 'Carga por URL:' %>
	<%= form.text_field :remote_image_url, id: :remote_image_url %>
	
</div>

<div class="actions">
	<%= form.submit 'Guardar', class:'btn btn-success' %>
</div>
<% end %>
